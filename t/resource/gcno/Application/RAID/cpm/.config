cpm_PARENTAGE =         RAID Application RC
cpm_PARENT_PATHS =      $(BASEDIR)/Application/RAID $(BASEDIR)/Application
UPSTREAM_CONFIG := $(if $(UPSTREAM_CONFIG),../$(UPSTREAM_CONFIG),../.config)
include $(UPSTREAM_CONFIG)
$(UPSTREAM_CONFIG):: ;
